server:
  port: ${PORT:8080}

spring:
  application:
    name: ${SPRING_APP_NAME:LiveInterview}

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: ${DB_DRIVER}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    jwt:
      secret: ${JWT_SECRET}
      expiration: ${JWT_EXPIRATION}

  sql:
    init:
      platform: ${SQL_PLATFORM}

livekit:
  api:
    key: ${LIVEKIT_API_KEY:devkey}
    secret: ${LIVEKIT_API_SECRET:secret}
  url: ${LIVEKIT_URL:ws://localhost:7880}
  token:
    ttl: ${LIVEKIT_TOKEN_TTL:7200}

internal:
  service:
    api-key: ${INTERNAL_SERVICE_API_KEY:internal-secret-key}

resume:
  normalization:
    service:
      url: ${RESUME_NORMALIZATION_SERVICE_URL:http://localhost:8000}

file:
  upload-dir: ${FILE_UPLOAD_DIR:uploads/resumes}

debug: true